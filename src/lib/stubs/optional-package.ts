/**
 * Stub module for optional packages that may not be installed.
 * Used by Turbopack resolveAlias to prevent build failures
 * when packages like dockerode, ioredis, or @xterm/* are not available.
 */
export default null;
export {};
