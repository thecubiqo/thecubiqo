// Force Vercel deployment by creating a new commit
const { execSync } = require('child_process');
const fs = require('fs');
const path = require('path');

console.log('🚀 FORCING VERCEL DEPLOYMENT FROM MAIN BRANCH');
console.log('=============================================\n');

try {
  // 1. Check current git status
  console.log('1. 📊 CHECKING CURRENT GIT STATUS:\n');
  
  const currentBranch = execSync('git branch --show-current', { cwd: __dirname }).toString().trim();
  console.log(`   Current branch: ${currentBranch}`);
  
  const lastCommit = execSync('git log --oneline -1', { cwd: __dirname }).toString().trim();
  console.log(`   Last commit: ${lastCommit}`);
  
  // 2. Check if we're on main branch
  if (currentBranch !== 'main') {
    console.log(`\n❌ NOT ON MAIN BRANCH! Switching to main...`);
    execSync('git checkout main', { cwd: __dirname });
    console.log('✅ Switched to main branch');
  }
  
  // 3. Create a new commit to trigger deployment
  console.log('\n2. 🔄 CREATING DEPLOYMENT-TRIGGERING COMMIT:\n');
  
  // Create a simple file to commit
  const triggerFile = path.join(__dirname, 'DEPLOYMENT_TRIGGER.md');
  const triggerContent = `# Deployment Trigger
Created: ${new Date().toISOString()}
Purpose: Force Vercel auto-deployment
Commit: ${lastCommit.split(' ')[0] || 'unknown'}

This file triggers Vercel to deploy the fixes that are already in main branch.

## Fixes included:
1. ✅ Moved app directory from src/app to root app (Next.js 16 compatibility)
2. ✅ Removed invalid 'dir' option from next.config.js
3. ✅ Added Supabase environment variables (placeholders)
4. ✅ Installed missing @fontsource/inter dependency
5. ✅ Fixed React hydration errors
6. ✅ Fixed 404 content in component tree

## Expected outcome:
- Site loads without errors
- EnergyCube animations work
- FoundersPass login works (PIN: 2026)
- All core features functional

## Next steps after deployment:
1. Add real Supabase keys to Vercel environment variables
2. Add other required API keys (OpenAI, Anthropic, etc.)
3. Deploy Social Army to Railway
4. Test all integrations

---
**Auto-generated by Clawdbot**`;
  
  fs.writeFileSync(triggerFile, triggerContent);
  console.log('✅ Created DEPLOYMENT_TRIGGER.md file');
  
  // 4. Add and commit
  execSync('git add DEPLOYMENT_TRIGGER.md', { cwd: __dirname });
  console.log('✅ Added file to git');
  
  const commitMessage = `chore: Trigger Vercel deployment - cubiqo.ai production fixes`;
  execSync(`git commit -m "${commitMessage}"`, { cwd: __dirname });
  console.log('✅ Created commit');
  
  // 5. Push to trigger deployment
  console.log('\n3. 🚀 PUSHING TO TRIGGER VERCEL DEPLOYMENT:\n');
  
  const pushOutput = execSync('git push origin main', { cwd: __dirname }).toString();
  console.log('✅ Push output:');
  console.log(pushOutput.substring(0, 500) + '...');
  
  // 6. Check what should happen
  console.log('\n4. ⏱️  VERCEL DEPLOYMENT TIMELINE:\n');
  
  console.log('Vercel should now:');
  console.log('1. ✅ Detect new commit on main branch');
  console.log('2. ✅ Start automatic deployment');
  console.log('3. ✅ Build the application');
  console.log('4. ✅ Deploy to production (cubiqo.ai)');
  console.log('5. ✅ Complete within 2-5 minutes');
  
  console.log('\n🔍 CHECK DEPLOYMENT STATUS:');
  console.log('1. Go to: https://vercel.com/cubiqo-projects-d7156840/cubiqo-repo');
  console.log('2. Click "Deployments" tab');
  console.log('3. Look for new deployment (should be in progress)');
  
  console.log('\n🌐 CHECK SITE STATUS (in 5 minutes):');
  console.log('1. Visit: https://cubiqo.ai');
  console.log('2. Check if site loads without errors');
  console.log('3. Verify EnergyCube animations');
  console.log('4. Test FoundersPass: https://www.cubiqo.ai/founderspass');
  console.log('5. PIN: 2026');
  
  console.log('\n🎯 EXPECTED OUTCOME:');
  console.log('✅ Site loads without React hydration errors');
  console.log('✅ No 404 content in component tree');
  console.log('✅ EnergyCube renders correctly');
  console.log('✅ All core features work');
  
  console.log('\n⚠️  NOTE: Some features still need API keys:');
  console.log('• Supabase: Need real keys in Vercel environment variables');
  console.log('• OpenAI/Anthropic: Need API keys for AI features');
  console.log('• ElevenLabs: Need API key for voice synthesis');
  console.log('• Social Army: Needs separate Railway deployment');
  
  console.log('\n🔧 NEXT STEPS AFTER DEPLOYMENT:');
  console.log('1. Add real API keys to Vercel environment variables');
  console.log('2. Test all core features');
  console.log('3. Deploy Social Army to Railway');
  console.log('4. Configure remaining integrations');
  
  console.log('\n🎉 DEPLOYMENT TRIGGERED!');
  console.log('Check Vercel dashboard in 1-2 minutes for deployment progress.');
  console.log('Check https://cubiqo.ai in 5 minutes for site status.');
  
} catch (error) {
  console.log(`❌ Error: ${error.message}`);
  console.log('\n🔧 MANUAL FALLBACK:');
  console.log('Please go to Vercel dashboard and manually trigger deployment:');
  console.log('1. https://vercel.com/cubiqo-projects-d7156840/cubiqo-repo');
  console.log('2. Click "Deployments"');
  console.log('3. Click "Trigger Deployment" or "Redeploy" on latest');
}