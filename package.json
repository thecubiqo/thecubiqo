{
  "name": "cubiqo-root",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "install": "cd frontend && yarn install",
    "build": "cd frontend && yarn build"
  }
}
